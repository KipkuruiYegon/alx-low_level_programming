#include "main.h"
/**
 * _abs - calculate absolute value of an integer
 * @n: int input for check
 * Return: absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
