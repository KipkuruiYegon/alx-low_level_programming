#include <stdio.h>
/**
 * main - Entry point for the program to print a line with the put function
 *
 * Return: always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
